// In the template below, replace each instance of
// /*____*/
// with the most appropriate data type for that variable.

#include <iostream>
using namespace std;

int main() {

   bool alpha = true;
   int beta = 36;
   char gamma = 'Q';
   double delta = 3.14159;
   
   cout << alpha << endl << beta << endl << gamma << endl << delta << endl;

   return 0;
}
